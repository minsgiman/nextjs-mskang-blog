import type { NextPage } from "next";
import Head from "next/head";

const Profile: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Profile</title>
        <meta name="description" content="mskang Profile" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div>Profile Page</div>
    </div>
  );
};

export default Profile;
