import type { NextPage } from "next";
import Head from "next/head";

const Photo: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Photo</title>
        <meta name="description" content="mskang Photo" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div>Photo Page</div>
    </div>
  );
};

export default Photo;
